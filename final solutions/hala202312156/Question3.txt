import 'package:flutter/material.dart';
import 'dart:math';

void main() {
  runApp(MyApp());
}

class MyApp extends StatefulWidget {
  const MyApp({super.key});

  @override
  State<MyApp> createState() => _MyAppState();
}

class _MyAppState extends State<MyApp> {
  bool big = false;
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      appBar
        home: Scaffold(
            body: Center(
                child: Container(
                  padding: EdgeInsets.all(0),
    child: Container(
    center(
    GestureDetector(
    onTap: () {
    setState(() {
    big = !big;    
    )
    width: 500,
    height: 500,
                  child: Image(
                      width: 200,
                      height: 200,
                      image: NetworkImage(
                          "data:image/hhtps://picsum.photos/200,
                  child: Column(
                      children: [
                      GestureDetector(
                      onTap: () {
                setState(() {
                big = !big;
                });
                },
                  child: AnimatedDefaultTextStyle(
                    child: Text("ASU"),
                    style: TextStyle(fontSize: f ? 30 : 100),
                    duration: Duration(seconds: 30),


                  children: [
              Transform.rotate(
              angle: 30 * (pi / 180),
              child: Text("Flutter Animated App")),
    
            ),