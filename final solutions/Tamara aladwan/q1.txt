import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}


class FlutterAnimatedApp extends StatefulWidget {
  const FlutterAnimatedApp({super.key});

  @override
  State<FlutterAnimatedApp> createState() => _FlutterAnimatedAppState();
}

class _FlutterAnimatedAppState extends State<FlutterAnimatedApp> {
  bool big=false;
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: const Text("Flutter Animated app"),),
        body:  Column(
          children: [
            AnimatedContainer(duration: Duration(seconds: 3)),
      width: big? 600:300,
      height: big?800:500,
      decoration: BoxDecoration(
          color: Colors.grey
      ),
      child: Image.network("https://picsum.photos/200"),
    ),
            Center(
              child: GestureDetector(
                onTap: (){
                  setState(() {
                    big=!big;
                  });
                },
                child: SingleChildScrollView(
                  child: Container(
                    child: Padding(padding:EdgeInsets.all(0.0),
                    ),
                  ),
                ),
              ),
            ),
            TextButton(onPressed: (){
    setState(() {
    big=!big;
    });
    },
    child: Text("Animated"),
    ),
          ], Padding(padding: const EdgeIsets.all(0.0),
    child: TextFormField(
    decoration: InputDecoration(
    labelText: 'Enter time (SS or MM:SS)'
    border: OutlineInputBorder(),
        ),
      ),
            
      